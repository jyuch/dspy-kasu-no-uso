[project]
name = "dspy-kasu-no-uso"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dspy>=3.0.4",
    "mlflow>=3.7.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.9",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
